/*
  회차 목적: 어떻게 값을 variable(변수)에 저장하고 나중에 사용하는가?
*/

const a = 5;
const b = 2;
const my name = "nico"; // Error
const myName = "nico"; // 자바스크립트 변수명에 띄어쓰기가 필요하면 다음 단어의 첫 문자를 대문자로 쓴다 (캐멀 표기법)
const veryLongVariableName = 0; // example

console.log(a + b);
console.log(a * b);
console.log(a / b);
console.log("hello " + myName);

// 파이썬에서는?
very_long_variable_name = 0; // (스네이크 케이스 / 밑줄 표기법)
